# xorg.conf (X.Org X Window System server configuration file)
#
# This file was generated by dexconf, the Debian X Configuration tool, using
# values from the debconf database.
#
# Edit this file with caution, and see the xorg.conf manual page.
# (Type "man xorg.conf" at the shell prompt.)
#
# This file is automatically updated on xserver-xorg package upgrades *only*
# if it has not been modified since the last upgrade of the xserver-xorg
# package.
#
# Note that some configuration settings that could be done previously
# in this file, now are automatically configured by the server and settings
# here are ignored.
#
# If you have edited this file but would like it to be automatically updated
# again, run the following command:
#   sudo dpkg-reconfigure -phigh xserver-xorg

Section "Monitor"
	Identifier	"Configured Monitor"

# DED: added following Modelines in attempt to get external 22" working, but
# perhaps it works without them?

#	Modeline "1600x1024_60.00"  136.36  1600 1704 1872 2144  1024 1025 1028 1060  -HSync +Vsync
#	Modeline "1600x1050_60.00"  139.83  1600 1704 1872 2144  1050 1051 1054 1087  -HSync +Vsync
	# 1600x1024 @ 60.00 Hz (GTF) hsync: 63.60 kHz; pclk: 136.36 MHz
	# 1600x1050 @ 60.00 Hz (GTF) hsync: 65.22 kHz; pclk: 139.83 MHz
EndSection

Section "Screen"
	Identifier	"Default Screen"
	Monitor		"Configured Monitor"
	Device		"Configured Video Device"
	SubSection "Display"
		Virtual	2048 1368
	EndSubSection
EndSection

Section "Device"
	Identifier	"Configured Video Device"
EndSection

# Try to disable Ctrl-Alt-Backspace
# or is it shift-alt-backspace?
# this has prevented X from starting in the past.
Section	"ServerFlags"
	Option "DontZap" "true"
EndSection
